export interface CreateTextToSpeechConfig {
  text_speech: string;
  code: string;
  description: string;
}

export interface UpdateTextToSpeechConfig {
  text_speech: string;
  code: string;
  description: string;
}